
<%= @title %> =
  <%- if @server.length == 1 -%>
  <%- @server.each_pair do |key, server| -%>
  (ADDRESS = (PROTOCOL = <%= server['protocol'] %>)(HOST = <%= server['host'] %>)(PORT = <%= server['port'] %>))
  <%- end -%>
  <%- else -%>
  (ADDRESS_LIST=
   <%- @server.each_pair do |key, server| -%>
    (ADDRESS = (PROTOCOL = <%= server['protocol'] %>)(HOST = <%= server['host'] %>)(PORT = <%= server['port'] %>))
     <%- end -%>
    )
  <%- end -%>
